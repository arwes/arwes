{
  "name": "@arwes/app-website",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^16.14",
    "npm": "^8.3"
  },
  "dependencies": {
    "motion": "^10.7.0",
    "next": "^12.1.4"
  },
  "scripts": {
    "clean": "rm -rf ./.next && rm -rf ./build",
    "dev": "next dev -p 9200",
    "build": "next build && next export",
    "start": "http-server ./out --port 9200 --gzip true -o /"
  }
}
